import React from "react";

const Loading = () => {
    return (
        <div className="w-screen h-screen flex justify-center items-center font-relative text-2xl bg-accent3">
            <div>Loading</div>
        </div>
    );
};

export default Loading;
